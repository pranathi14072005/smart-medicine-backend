spring.application.name=CareMeds
server.port=8082

spring.datasource.driver-class-name=org.postgresql.Driver


spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PW}


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true	

spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreQLDialect
spring.jpa.open-in-view=false

#app.jwt.secret=SmartMedicineSecretKey2024VeryLongAndSecureSecretKeyForJWTTokenGeneration
#app.jwt.expiration=86400000
#app.jwt.refresh-expiration=604800000

app.jwt.expiration=${JWT_SECRET}
app.jwt.refresh-expiration=${JWT_EXPIRATION}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pranathipaidi143@gmail.com
spring.mail.password=fhzqpifnpevsbtef
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

logging.level.com.smartmedicine=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG

management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

app.cors.allowed-origins=http://localhost:3000,http://localhost:4200,http://localhost:3001
springdoc.api-docs.path=/api-docs

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# ─── Firebase Cloud Messaging (DISABLED) ───────────────────────────────────────
# firebase.config.path=classpath:firebase-service-account.json

# Enable Spring scheduled tasks (reminder dispatch)
spring.task.scheduling.pool.size=5